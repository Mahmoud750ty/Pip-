import React from 'react';
import ProductPage from '../../components/admin/ProductPage';

const VibeSavePage: React.FC = () => {
    return <ProductPage categoryName="Vibe Save" collectionName="vibe-save" />;
};

export default VibeSavePage;